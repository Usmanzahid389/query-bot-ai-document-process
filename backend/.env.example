DATABASE_URL=sqlite+aiosqlite:///./querybot.db
JWT_SECRET=change-me-to-a-long-random-string
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
# UPLOAD_DIR=./uploads
# MAX_UPLOAD_BYTES=10485760

# PDF parsing: auto (try OpenDataLoader, fallback PyMuPDF) | opendataloader | pymupdf
# OpenDataLoader needs Java on PATH; better layout/tables/multi-column than PyMuPDF alone.
# PDF_PARSER=auto

# RAG: OpenAI-compatible LLM (OpenRouter, Groq, etc.)
# CHROMA_DIR=./chroma_db
# CHROMA_COLLECTION=querybot_documents
# EMBEDDING_MODEL_ID=sentence-transformers/all-MiniLM-L6-v2
# OpenRouter example:
# LLM_BASE_URL=https://openrouter.ai/api/v1
# LLM_MODEL=meta-llama/llama-3.3-70b-instruct:free
# Groq example (model IDs differ — see https://console.groq.com/docs/models):
# LLM_BASE_URL=https://api.groq.com/openai/v1
# LLM_MODEL=llama-3.3-70b-versatile
# LLM_API_KEY=your_api_key
# RAG_CHUNK_SIZE=800
# RAG_CHUNK_OVERLAP=160
# RAG_TOP_K=12
# RAG_SUMMARY_TOP_K=10
# MMR: pull RAG_FETCH_K candidates, diversify, keep RAG_TOP_K (same embedding model; light CPU).
# RAG_FETCH_K=28
# RAG_MMR_ENABLED=true
# RAG_MMR_LAMBDA=0.55
# RAG_SUMMARY_FETCH_K=
# Stronger retrieval: try a larger EMBEDDING_MODEL_ID (heavier RAM/index time).
